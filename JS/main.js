// Make sure function is accessible globally
window.openGame = function(gameName) {
    alert(`${gameName} is now loading!`);
    // Future: You can replace this with actual game opening logic
};
